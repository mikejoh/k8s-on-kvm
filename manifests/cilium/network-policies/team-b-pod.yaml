apiVersion: v1
kind: Pod
metadata:
  labels:
    team: b
  name: team-b-nginx
  namespace: team-b
spec:
  containers:
    - image: nginx
      name: nginx
      resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Never
