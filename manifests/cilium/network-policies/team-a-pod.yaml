apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
    team: a
  name: team-a-nginx
  namespace: team-a
spec:
  containers:
    - image: nginx
      name: nginx
      resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Never
